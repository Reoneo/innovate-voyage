import React from 'react';

interface FollowStatsProps {
  walletAddress: string;
}

const FollowStats: React.FC<FollowStatsProps> = ({ walletAddress }) => {
  // Component removed for speed optimization
  return null;
};

export default FollowStats;
