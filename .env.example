
# GitHub API token - get one from https://github.com/settings/tokens
# IMPORTANT: This token has likely been revoked because it was committed to a public repo
# Please create a new token with the following scopes: read:user, repo
# Classic tokens with read:user scope are sufficient for public repos
GITHUB_API_TOKEN=your_github_personal_access_token_here

# Optional: Server port (defaults to 4000)
# PORT=4000
